{
  "name": "idealtwin-app",
  "version": "1.0.0",
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
"scripts": {
  "build": "mkdir -p dist && touch dist/index.html",
  "ionic:build": "npm run build",
  "ionic:cap-copy": "npx cap copy ios",
  "ionic:cap-sync": "npx cap sync ios"
}
}